1.附件中的程序共有4个错误，请通过设置断点debug改成其中的错误。
2.编写书中例题8-5，并以注释形式回答以下问题：
   A. 一个try有多个对应catch的情况，当异常发生的时候是否会执行多个catch块，选择执行哪个catch块的规则是什么。
   B. throw和throws的区别是什么，分别用在什么地方？
   C. 当try...catch块嵌套的情况下，内部try.catch块出现异常，是否有可能导致外层catch代码执行？为什么？
   D. 在函数func1中调用了函数func2，函数func2调用了func3，当func3中发生异常，虚拟机寻找catch块的顺序是什么？
   E. final和finally的区别是什么？一般在什么情况下会使用finally
3. 在程序中让用户输入形如"2018-09-01 22:12:50"的字符串，将其转换为Date类型，并计算：
   A。该日期是星期几
   B. 该日期距日期当年1月1日0点0分的秒数。
   C。该日期150天后的日期
   打印输出上述计算结果，其中C的输出格式应形如"2019/2/5 22:5:9"
